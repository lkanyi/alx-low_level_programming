#include "0-object_like_macro.h"
#include <stdio.h>
#define BUFFER_SIZE 1024

/**
 * header file- defines macro
 * size = 1024
 */

int main (void)
{
	int s;
	s = 1024 + size;
	
	("%d\n", s);
	
	return (0);
}

